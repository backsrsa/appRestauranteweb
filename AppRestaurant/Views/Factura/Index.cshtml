@model IEnumerable<AppRestaurant.Models.tblFactura>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FechaFactura)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblCamarero.Nombres)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblCliente.Nombres)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblMesa.Ubicacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FechaFactura)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblCamarero.Nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblCliente.Nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblMesa.Ubicacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFactura }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFactura }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFactura })
        </td>
    </tr>
}

</table>
